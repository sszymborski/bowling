#ifndef INTERFACE_H
#define INTERFACE_H

#include <iostream>

using namespace std;


class interface
{
    public:
        interface();
        virtual ~interface();
        int start();
        int okresl();
    protected:
    private:
};

#endif // INTERFACE_H
